/**
 * seed.js — Supabase Edition
 * Dữ liệu mẫu đã được seed bằng SUPABASE_SETUP.sql (chạy trong Supabase Dashboard).
 * File này là no-op — isSeeded() luôn trả về true từ DB.isSeeded().
 */
async function seedIfNeeded() {
  // Không cần seed — dữ liệu đến từ Supabase, đã chạy SQL setup sẵn.
  return;
}
